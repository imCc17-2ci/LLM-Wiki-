# 隐私发布清单

在执行 `git add` 前检查：

- [ ] 没有真实 `raw/` 素材、作品全文、课程笔记或网页剪藏。
- [ ] 没有真实概念页、实体页、写作技巧或私有方法论。
- [ ] 没有实际查询报告、草稿、项目产物或本地日志。
- [ ] 没有姓名、联系方式、简历、账号、密钥或 `.obsidian/` 数据。
- [ ] 暂存区只包含 README、许可证、公开工作流文档与 `public-template/` 的虚构示例。

建议使用 `git diff --cached --stat` 与 `git diff --cached` 做最终人工复核。
